apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: timoreimann/kubeadm-control-plane-controller-amd64:v0.4.0-fixes
          name: manager
